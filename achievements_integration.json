{
  "gameId": "drift",
  "integrationStatus": "complete",
  "lastUpdated": "2026-02-24T00:00:00Z",
  "achievements": [
    {
      "id": "first_lap",
      "trigger": "lap_completed signal — any player completes a lap",
      "hook": "race_manager._on_achievement_lap_completed()",
      "status": "implemented"
    },
    {
      "id": "first_race",
      "trigger": "Race end — _on_race_finished()",
      "hook": "AchievementManager.try_unlock in _on_race_finished()",
      "status": "implemented"
    },
    {
      "id": "drift_king",
      "trigger": "drift_released signal — drift_charge_time >= 3.0",
      "hook": "race_manager._on_achievement_drift_check()",
      "status": "implemented"
    },
    {
      "id": "speed_demon",
      "trigger": "speed_changed signal — speed >= CAR_MAX_SPEED",
      "hook": "race_manager._on_achievement_speed_check()",
      "status": "implemented"
    },
    {
      "id": "clean_run",
      "trigger": "Race end — winner car.wall_hit_count == 0",
      "hook": "AchievementManager.try_unlock in _on_race_finished()",
      "status": "implemented"
    },
    {
      "id": "checkpoint_ace",
      "trigger": "lap_completed signal — lap completion requires all checkpoints",
      "hook": "race_manager._on_achievement_lap_completed()",
      "status": "implemented"
    },
    {
      "id": "personal_best",
      "trigger": "Race end — new_best_lap && prev_best_lap != INF",
      "hook": "AchievementManager.try_unlock in _on_race_finished()",
      "status": "implemented"
    },
    {
      "id": "track_master",
      "trigger": "Race end — LapHistory.get_total_race_count() >= 10",
      "hook": "AchievementManager.try_unlock in _on_race_finished()",
      "status": "implemented"
    }
  ],
  "notes": "AchievementManager is a static class (class_name AchievementManager, extends RefCounted). It loads/saves achievements.json at repo root. Toast notifications handled by ui_controller.show_achievement_toast(). Callbacks registered in race_manager._ready() with stale-callback safety via clear_callbacks()."
}
